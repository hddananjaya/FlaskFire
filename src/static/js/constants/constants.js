const NotificationType = Object.freeze({
  "KEYPRESS": "KEYPRESS",
  "JOIN": "JOIN",
  "EXIT": "EXIT",
});

const ChatItemType = Object.freeze({
  "MESSAGE": "MESSAGE",
  "NOTIFICATION": "NOTIFICATION",
});

export {
  NotificationType,
  ChatItemType,
}